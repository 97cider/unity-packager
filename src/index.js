'use strict';

const fs = require('fs');
var path = require('path');
const { exec, spawn } = require('child_process');

buildCommandFromOptions = (options) => {
    // build unity build from options
}